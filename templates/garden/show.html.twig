{% extends 'base.html.twig' %}

{% block title %}Garden{% endblock %}

{% block body %}
    <h1>Garden</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ garden.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ garden.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ garden.createdAt ? garden.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ garden.updatedAt ? garden.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('garden_index') }}">back to list</a>

    <a href="{{ path('garden_edit', {'id': garden.id}) }}">edit</a>

    {{ include('garden/_delete_form.html.twig') }}
{% endblock %}
