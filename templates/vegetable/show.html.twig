{% extends 'base.html.twig' %}

{% block title %}Vegetable{% endblock %}

{% block body %}
    <h1>Vegetable</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vegetable.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vegetable.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vegetable_index') }}">back to list</a>

    <a href="{{ path('vegetable_edit', {'id': vegetable.id}) }}">edit</a>

    {{ include('vegetable/_delete_form.html.twig') }}
{% endblock %}
